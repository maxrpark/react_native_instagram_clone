import React from 'react';
import {View, Text} from 'react-native';

type Props = {};

const ProfileReels: React.FC = (props: Props) => {
  return <Text>ProfileReels</Text>;
};

export default ProfileReels;
