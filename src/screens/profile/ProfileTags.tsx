import React from 'react';
import {View, Text} from 'react-native';

type Props = {};

const ProfileTags: React.FC = (props: Props) => {
  return (
    <View style={{flex: 1, backgroundColor: 'red'}}>
      <Text>ProfileTags</Text>
    </View>
  );
};

export default ProfileTags;
