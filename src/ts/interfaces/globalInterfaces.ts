export interface StoriesInt {
  id: string;
  src: string;
  user: string;
}

export interface PostData {
  user: string;
  avatar: string;
  desc: string;
  url: string;
  saved: boolean;
  liked: boolean;
}
