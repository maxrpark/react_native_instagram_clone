export interface StoriesInt {
  ids: string;
  src: string;
  user: string;
}

export interface PostData {
  user: string;
  avatar: string;
  desc: string;
  url: string;
  saved: boolean;
  liked: boolean;
}
