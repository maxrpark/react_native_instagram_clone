import {SafeAreaView} from 'react-native';
import {FeedScreen} from './src/screens';
const App = () => {
  return (
    <SafeAreaView>
      <FeedScreen />
    </SafeAreaView>
  );
};

export default App;
